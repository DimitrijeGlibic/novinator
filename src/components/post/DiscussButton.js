import Button from 'react-bootstrap/Button';

const DiscussButton = () => {
    return (
        <div className="d-grid gap-2">
            <Button variant="outline-primary">Discuss</Button>
        </div>
    );
};

export default DiscussButton;